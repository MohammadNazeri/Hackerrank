List Comprehensions: new_list = [expression for item in iterable if condition]
    nums = [[i, j, k] for i in range(x+1) for j in range(y+1) for k in range(z+1) if (i+j+k) != n]
Nested list: multi-dimensional array
    nested_list = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
map(): is a built-in function that applies a given function to each item in an iterable
    map(function, iterable)
    squared_list = map(lambda x: x ** 2, list)
split&join
    "-".join(lines)
collections.Counter() is a dict where elements are stored as dictionary keys and their counts are stored as dictionary values.
    Counter(myList)
